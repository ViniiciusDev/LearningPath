// Loop For - Repete até que a condição seja FALSE

/* for (step = 0; step < 10; step++) {
    console.log(step);
} */

// EXEMPLO DE TABUADA

let number = 7
for (step = 0; step <= 10; step++) {
    console.log(`${number} x ${step} = ${number * step}`);
}