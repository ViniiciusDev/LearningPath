let fruits = ["Apple", "Banana", "Orange"]
console.log(fruits);

// Tamanho do nosso array
console.log(fruits.length);

// Acessando o nosso Array
console.log(fruits[0]);

// Obtém dinamicamente o ultimo item.
console.log(fruits[fruits.length - 1]);

