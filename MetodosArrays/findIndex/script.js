// Método findIndex() retorna o índice do array do primeiro elemento que satisfaz a condição, retorna -1, indicando que nenhum elemento passou no teste.

const values = [10, 20, 30, 40, 50];
console.log(values.findIndex((value) => value > 20));
